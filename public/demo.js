document.getElementById('demoSignup').addEventListener('submit',async e=>{e.preventDefault();const form=e.target,button=form.querySelector('button'),status=document.getElementById('result');button.disabled=true;status.textContent='Sending your invitation…';try{const data=Object.fromEntries(new FormData(form));data.contactConsent=form.elements.contactConsent.checked;const r=await fetch('/api/demo-request',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const d=await r.json();if(!r.ok)throw Error(d.error||'Please try again.');status.textContent=d.message;form.hidden=true;}catch(err){status.textContent=err.message;button.disabled=false;}});
